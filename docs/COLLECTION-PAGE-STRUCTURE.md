# Collection Page Structure Diagram

## Visual Layout

```
┌─────────────────────────────────────────────────────────────┐
│                     HERO BANNER (50vh)                      │
│                                                             │
│              [Collection Background Image]                  │
│                     opacity: 0.8                            │
│                                                             │
│                  ┌─────────────────┐                        │
│                  │  COLLECTION     │                        │
│                  │     TITLE       │  (Centered Overlay)    │
│                  │  (Italic Bold)  │                        │
│                  └─────────────────┘                        │
│                                                             │
└─────────────────────────────────────────────────────────────┘
┌─────────────────────────────────────────────────────────────┐
│  STICKY FILTER BAR (position: sticky, top: 0)              │
│  ┌──────────────────────────┬──────────────────────────┐   │
│  │ [Filter] 30 Products     │              [Sort]      │   │
│  └──────────────────────────┴──────────────────────────┘   │
└─────────────────────────────────────────────────────────────┘
┌─────────────────────────────────────────────────────────────┐
│                    PRODUCT GRID                             │
│  ┌──────┬──────┬──────┬──────┐  ← 1px gray gaps            │
│  │      │      │      │      │                              │
│  │ IMG  │ IMG  │ IMG  │ IMG  │  ← 4 columns desktop         │
│  │      │      │      │      │                              │
│  ├──────┼──────┼──────┼──────┤                              │
│  │Title │Title │Title │Title │                              │
│  │Price │Price │Price │Price │                              │
│  └──────┴──────┴──────┴──────┘                              │
│  ┌──────┬──────┬──────┬──────┐                              │
│  │      │      │      │      │                              │
│  │ IMG  │ IMG  │ IMG  │ IMG  │  ← Hover: Image swap         │
│  │      │      │      │      │    (0.7s transition)         │
│  ├──────┼──────┼──────┼──────┤                              │
│  │Title │Title │Title │Title │                              │
│  │Price │Price │Price │Price │                              │
│  └──────┴──────┴──────┴──────┘                              │
│                                                             │
│              [LOAD MORE BUTTON]                             │
│                                                             │
└─────────────────────────────────────────────────────────────┘
```

## Responsive Breakpoints

### Mobile (< 768px)
```
┌─────────────────────┐
│   HERO (50vh)       │
└─────────────────────┘
┌─────────────────────┐
│  FILTER BAR         │
└─────────────────────┘
┌─────────┬─────────┐
│  IMG    │  IMG    │  ← 2 columns
├─────────┼─────────┤
│ Title   │ Title   │
│ Price   │ Price   │
└─────────┴─────────┘
```

### Tablet (768px - 1023px)
```
┌───────────────────────────────┐
│       HERO (50vh)             │
└───────────────────────────────┘
┌───────────────────────────────┐
│      FILTER BAR               │
└───────────────────────────────┘
┌─────────┬─────────┬─────────┐
│  IMG    │  IMG    │  IMG    │  ← 3 columns
├─────────┼─────────┼─────────┤
│ Title   │ Title   │ Title   │
│ Price   │ Price   │ Price   │
└─────────┴─────────┴─────────┘
```

### Desktop (1024px+)
```
┌─────────────────────────────────────────────┐
│           HERO (50vh)                       │
└─────────────────────────────────────────────┘
┌─────────────────────────────────────────────┐
│          FILTER BAR                         │
└─────────────────────────────────────────────┘
┌──────┬──────┬──────┬──────┐
│ IMG  │ IMG  │ IMG  │ IMG  │  ← 4 columns
├──────┼──────┼──────┼──────┤
│Title │Title │Title │Title │
│Price │Price │Price │Price │
└──────┴──────┴──────┴──────┘
```

## Component Hierarchy

```
RepresentCollectionPage
├── Hero Banner
│   ├── Background Image (collection.image)
│   └── Title Overlay (collection.title)
│
├── Sticky Filter Bar
│   ├── Left Section
│   │   ├── Filter Button + Icon
│   │   └── Product Count
│   └── Right Section
│       └── Sort Button + Icon
│
└── Product Grid (Pagination wrapper)
    ├── RepresentProductCard (x30)
    │   ├── Image Wrapper
    │   │   ├── Primary Image (opacity: 1)
    │   │   └── Secondary Image (opacity: 0)
    │   └── Card Info
    │       ├── Title
    │       └── Price
    └── Load More Button
```

## Image Swap Mechanism

### Default State
```
┌─────────────────┐
│                 │
│   IMAGE 1       │  ← opacity: 1 (visible)
│   (Primary)     │
│                 │
└─────────────────┘
     ┌─────────────────┐
     │                 │
     │   IMAGE 2       │  ← opacity: 0 (hidden)
     │   (Secondary)   │
     │                 │
     └─────────────────┘
```

### Hover State
```
┌─────────────────┐
│                 │
│   IMAGE 1       │  ← opacity: 0 (hidden)
│   (Primary)     │
│                 │
└─────────────────┘
     ┌─────────────────┐
     │                 │
     │   IMAGE 2       │  ← opacity: 1 (visible)
     │   (Secondary)   │
     │                 │
     └─────────────────┘
```

**Both images are absolutely positioned in the same space**
**Only opacity changes (0.7s transition)**

## Grid Lines Technique

### Container Structure
```
┌─────────────────────────────────────┐  ← background: #e5e5e5
│ ┌─────┐ ┌─────┐ ┌─────┐ ┌─────┐   │
│ │     │ │     │ │     │ │     │   │  ← Cards: background: white
│ │Card │ │Card │ │Card │ │Card │   │
│ │     │ │     │ │     │ │     │   │
│ └─────┘ └─────┘ └─────┘ └─────┘   │
│    ↑ 1px gap (shows gray background)
└─────────────────────────────────────┘
```

### CSS Implementation
```css
.represent-product-grid {
  display: grid;
  gap: 1px;                    /* Creates space */
  background: #e5e5e5;         /* Shows through gap */
}

.represent-product-card {
  background: white;           /* Covers grid background */
}
```

## Animation Timeline

### Page Load
```
Time: 0s
┌─────────────────────────────────────┐
│  Hero Banner: Instant display      │
│  Filter Bar: Instant display       │
│  Products: opacity: 0, y: 30px     │  ← Hidden
└─────────────────────────────────────┘

Time: User scrolls to 80% viewport
┌─────────────────────────────────────┐
│  Products: Staggered fade-in        │
│  Card 1: 0.0s → opacity: 1, y: 0   │
│  Card 2: 0.1s → opacity: 1, y: 0   │
│  Card 3: 0.2s → opacity: 1, y: 0   │
│  Card 4: 0.3s → opacity: 1, y: 0   │
│  ...                                │
└─────────────────────────────────────┘
```

### Hover Interaction
```
Time: 0s (No hover)
┌─────────────┐
│   Image 1   │  opacity: 1
│   Image 2   │  opacity: 0
└─────────────┘

Time: 0.35s (Mid-transition)
┌─────────────┐
│   Image 1   │  opacity: 0.5
│   Image 2   │  opacity: 0.5
└─────────────┘

Time: 0.7s (Hover complete)
┌─────────────┐
│   Image 1   │  opacity: 0
│   Image 2   │  opacity: 1
└─────────────┘
```

## Data Flow

```
Route Loader
    ↓
GraphQL Query (Shopify Storefront API)
    ↓
Collection Data
    ├── id, handle, title, description
    ├── image {url, altText}
    └── products {nodes[], pageInfo}
        ↓
RepresentCollectionPage Component
    ├── Hero Banner (collection.image, collection.title)
    ├── Filter Bar (products.length)
    └── Product Grid
        ↓
    RepresentProductCard (x30)
        ├── images[0] → Primary Image
        ├── images[1] → Secondary Image
        ├── title → Card Title
        └── priceRange → Card Price
```

## Scroll Behavior

### Navigation from Homepage
```
Homepage Parallax
    ↓
User clicks collection link
    ↓
React Router navigates
    ↓
RepresentCollectionPage mounts
    ↓
useEffect runs: scrollToTop(true)
    ↓
Lenis scrolls to top (immediate)
    ↓
User sees hero banner at top
    ↓
User scrolls down
    ↓
Filter bar becomes sticky
    ↓
Products animate in (GSAP)
```

## File Structure

```
hydrogen-storefront/
├── app/
│   ├── components/
│   │   ├── RepresentCollectionPage.tsx  ← Main component
│   │   ├── ProductCard.tsx              ← Type definitions
│   │   └── smooth-scroll/
│   │       └── LenisProvider.tsx        ← Scroll management
│   ├── routes/
│   │   └── ($locale).collections.$handle.tsx  ← Route
│   ├── lib/
│   │   └── lenis.ts                     ← Scroll utilities
│   └── styles/
│       └── app.css                      ← Styles
└── docs/
    ├── REPRESENT-COLLECTION-PAGE.md     ← Full docs
    ├── COLLECTION-PAGE-QUICKSTART.md    ← Quick start
    └── COLLECTION-PAGE-STRUCTURE.md     ← This file
```

## Key Measurements

### Hero Banner
- Height: `50vh` (half viewport)
- Image opacity: `0.8`
- Title size: `3rem` → `5rem` → `8rem` (mobile → tablet → desktop)

### Filter Bar
- Padding: `1rem` vertical, `1.5rem` horizontal
- Font size: `10px`
- Letter spacing: `0.15em`
- Z-index: `40`

### Product Grid
- Gap: `1px`
- Background: `#e5e5e5`
- Columns: `2` → `3` → `4` (mobile → tablet → desktop)

### Product Card
- Aspect ratio: `3:4`
- Image transition: `0.7s ease-in-out`
- Info padding: `1.5rem`
- Title size: `11px`
- Price size: `10px`

### Animations
- Fade-in duration: `0.8s`
- Stagger delay: `0.1s`
- Ease: `power2.out`
- Trigger: `top 80%`

## Performance Metrics

### Target Scores
- Lighthouse Performance: > 90
- First Contentful Paint: < 1.5s
- Largest Contentful Paint: < 2.5s
- Cumulative Layout Shift: < 0.1

### Optimizations Applied
- Lazy image loading
- GPU-accelerated transitions (opacity)
- GSAP cleanup on unmount
- Responsive image sizes
- Client-side animation gating
